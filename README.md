# Agent_using_API

 command that activates the venv, installs the required packages, and runs the agent, use:

.\.venv\Scripts\python.exe -m pip install google-genai python-dotenv; .\.venv\Scripts\python.exe agent.py
